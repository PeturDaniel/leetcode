struct Solution;

impl Solution {
    pub fn min_bitwise_array(nums: Vec<i32>) -> Vec<i32> {
        let mut ans: Vec<i32> = vec![];
        for num in nums {}
    }
}

fn main() {
    let tests = vec![vec![2, 3, 5, 7], vec![11, 13, 31]];

    for &n in &tests {
        println!("{:?} -> {:?}", n, Solution::min_bitwise_array(n));
    }

    println!("Hello, world!");
}
